{
  "name": "rx-poller",
  "private": true,
  "version": "1.0.0",
  "description": "Poller using Rx with exponential backoff and configurable intervals.",
  "scripts": {
    "test-watch": "karma start --auto-watch --no-single-run --NODE_ENV=test",
    "test": "karma start --NODE_ENV=test"
  },
  "main": "dist/RxPoller.js",
  "typings": "dist/RxPoller.d.ts",
  "license": "MIT",
  "dependencies": {
    "rx": "^4.0.8"
  },
  "devDependencies": {
    "jasmine": "2.4.1",
    "karma": "0.13.19",
    "karma-chrome-launcher": "~0.2.3",
    "karma-coverage": "0.5.3",
    "karma-jasmine": "0.3.6",
    "karma-sourcemap-loader": "0.3.7",
    "karma-spec-reporter": "0.0.23",
    "karma-typescript-preprocessor": "0.0.21",
    "typescript": "~1.8.5"
  }
}
